# ⚡ LightWidget — Мониторинг отключений света (macOS, Windows & iPhone)

Стильный виджет с темным Glassmorphism-интерфейсом для отслеживания времени отключения и восстановления электроэнергии на базе сообщений от Telegram-бота ДТЭК.

---

## 📋 История обновлений

### 🚀 Update 2.0
**[+] Добавлено:**
* Облачная синхронизация 24/7 через Render Server (`server.py`, `Dockerfile`, `Procfile`) — виджет на iPhone работает автономно без постоянно включенного ПК.
* Встроенный механизм KeepAlive для предотвращения засыпания бесплатного сервера.
* Надежное сквозное шифрование чувствительных данных (HMAC-SHA256).
* Шаблон `config.example.json` для удобной и безопасной настройки проекта.
* Полное сохранение всех пользовательских параметров отображения и уведомлений в конфигурации.
* Новые четкие векторные SVG-иконки разделов интерфейса.
* Поддержка многосоставных сообщений с автоматическим выбором самого позднего времени восстановления.

**[/] Изменено/пофикшено:**
* Исправлено: не отображался приглушенный фоновый свет (восстановлена плавная и глубокая подсветка внутри окна приложения).
* Исправлено: строка «Адрес» теперь изначально отображает «Не указан» вместо тестового адреса (подставляется автоматически при указании фильтра в Telegram или из первого сообщения).
* Переработана логика iOS-виджета: адаптивный компактный формат (`1д 4ч 10м` ➔ `4ч 10м` ➔ `15м` ➔ `35с`).
* Исправлена ошибка виджета iPhone, когда таймер зависал на 0 секундах или уходил в плюс вместо отображения статуса «Свет есть».
* Исправлен кэш iOS WidgetKit с помощью анти-кэш заголовков и локального резервного кэширования.
* Исправлен контраст элементов при выборе белого акцентного цвета (тумблеры, кнопки, бейджи шагов).
* Убрано красное свечение за внешними границами окна приложения — фон теперь светится строго внутри контейнера.
* Полная оптимизация и чистка всей кодовой базы проекта.

**[-] Удалено:**
* Удалены все отладочные комментарии и лишние пробелы из всех модулей (`app.py`, `server.py`, `core/*`, `ui/*`, `ios/*`).
* Удалено отслеживание приватных файлов авторизации, сессий Telegram и локальных баз данных из репозитория Git.
* Удалены эмодзи из заголовков интерфейса в пользу чистого векторного UI.

---

## 🚀 Возможности
- **Монитор реального времени**:
  - Живой статус питания (🟢 СВІТЛО Є / 🔴 ВІДКЛЮЧЕННЯ СВІТЛА).
  - Таймер обратного отсчета до ориентировочного восстановления (`ЧЧ:ММ:СС`).
  - Анимированная шкала прогресса времени отключения.
  - Детали: адрес, причина работ, точные таймстемпы начала и завершения.
- **Интеграция с iPhone (Scriptable)**:
  - Автоматическая синхронизация с облачным или локальным JSON API (`/api/status`).
  - Готовый JavaScript-код для создания нативного виджета на домашнем экране или экране блокировки iOS.
- **Тестовый симулятор**:
  - Возможность мгновенно протестировать любые сообщения бота ДТЭК, задержки или восстановление питания в 1 клик.
- **Telegram Listener (Telethon)**:
  - Фоновое получение сообщений от бота без необходимости ручной пересылки.
  - Фильтрация по конкретной улице / номеру дома.
- **Системные уведомления**:
  - Звуковые и визуальные баннеры при фиксации отключения или включения света.

---

## 💻 Запуск на Mac / ПК

```bash
cd LightWidget
python3 app.py
```

---

## 📱 Настройка на iPhone

1. Установите бесплатное приложение **Scriptable** из App Store на iPhone.
2. В приложении LightWidget перейдите во вкладку **"Виджет"** и нажмите **"Скопировать код виджета"**.
3. В приложении Scriptable на iPhone создайте новый скрипт (+), вставьте скопированный код и сохраните.
4. Добавьте виджет Scriptable на экран "Домой" или экран блокировки.
